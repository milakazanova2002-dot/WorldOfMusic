ПЛАН КОРРЕКТИРОВОК

🎯 Главная цель корректировок
Привести твой текущий проект к архитектуре WOM без разрушения существующего кода, постепенно, безопасно, с минимальными изменениями в каждом шаге.

🧩 Этап 1 — Синхронизация моделей User и профилей
1.1. Убрать зависимость от role
(но не сейчас — позже, когда мы подготовим профили)

1.2. Перевести логику ролей на профили
роль определяется наличием TeacherProfile / StudentProfile

пользователь может иметь оба профиля

1.3. Переписать сигнал education.signals
Сейчас он создаёт профиль по role.
В WOM профили создаются вручную или через интерфейс.

1.4. Добавить недостающие поля User
email_verified

is_approved
(у тебя уже есть — отлично)

🧩 Этап 2 — Приведение TeacherProfile и StudentProfile к WOM
2.1. TeacherProfile
Добавить поля WOM (позже, когда ты скажешь «можно править»):

middle_name

photo

education

telegram_url / vk_url

is_verified

is_public

created_at / updated_at

2.2. StudentProfile
Добавить поля WOM:

birth_date

is_public

created_at / updated_at

2.3. Убрать поле teacher из StudentProfile
В WOM связь педагог–ученик хранится в TeachingAssignment.

🧩 Этап 3 — Добавление модели TeachingAssignment
TeachingAssignment — ключевая модель WOM:

teacher → TeacherProfile

student → StudentProfile

subject → Subject

created_at

updated_at

Она заменяет:

поле teacher в StudentProfile

часть логики Lesson

🧩 Этап 4 — Приведение музыкальной библиотеки к WOM
4.1. Переименовать MusicalPiece → MusicPiece
(аккуратно, через миграции)

4.2. Genre → Category
(в WOM жанры — это категории)

4.3. Добавить недостающие поля MusicPiece
slug

difficulty

duration

is_public

created_at

updated_at

4.4. Добавить модель MusicMaterial
Материалы для произведений:

file

description

type

created_at

updated_at

piece → MusicPiece

4.5. Добавить модель Performance
Записи исполнения ученика:

assignment → TeachingAssignment

piece → MusicPiece

file

score

created_at

4.6. Добавить PerformanceComment
Комментарии педагога к исполнению.

🧩 Этап 5 — Пересмотр модели Lesson
❗ В WOM нет Lesson
Но твоя модель Lesson полезна для расписания.

Мы можем:

Вариант A — оставить Lesson как «расписание»
И связать её с TeachingAssignment.

Вариант B — удалить Lesson
Если расписание не нужно.

Ты сама выберешь позже.

🧩 Этап 6 — Пересборка URL‑ов и views
После корректировки моделей:

добавить страницы библиотеки,

добавить страницы педагогов,

добавить страницы учеников,

добавить страницы произведений,

добавить страницы материалов,

добавить страницы performance.

🧩 Этап 7 — Пересборка админки
После обновления моделей:

добавить фильтры,

добавить inline‑модели,

добавить поиск,

добавить отображение профилей,

добавить отображение материалов,

добавить отображение performance.

🧩 Этап 8 — Пересборка фронтенда
После обновления моделей:

библиотека станет динамической,

педагоги станут динамическими,

ученики станут динамическими,

произведения станут динамическими,

материалы станут динамическими.

🧩 Этап 9 — Финальная проверка
миграции,

админка,

URL‑ы,

шаблоны,

связи моделей,

тестовые данные.




Сделать, чтобы всегда было место для аватарки, а если аватарки нет, можно было выбрать из нескольких фоток или кружочка с первой буквы имени.


Чтобы во время сессии отображался всегда в хедере, чей это аккаунт.

Все эти элементы должны быть адаптивными и работать на разных устройствах.

Использование медиа-запросов для изменения стилей в зависимости от размера экрана.


Проверить работу всех элементов на различных устройствах и браузерах.Можно использовать CSS Grid или Flexbox для создания адаптивных сетей.


